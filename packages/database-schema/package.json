{
  "name": "@universal-pos/database-schema",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:migrate:dev": "prisma migrate dev",
    "db:seed": "npm run db:seed:build && prisma db seed",
    "db:seed:build": "tsc -p tsconfig.json",
    "db:push": "prisma db push",
    "db:studio": "prisma studio",
    "build": "prisma generate",
    "lint": "echo 'no lint'"
  },
  "prisma": {
    "seed": "node dist/prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.x",
    "bcrypt": "^5.x",
    "prisma": "^5.x"
  },
  "devDependencies": {
    "@types/bcrypt": "^5.x",
    "ts-node": "^10.x",
    "typescript": "^5.x"
  }
}
