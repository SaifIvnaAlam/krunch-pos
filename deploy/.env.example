# Copy to deploy/.env on the server (or locally before deploy-to-vps.sh).
# Do not commit deploy/.env.

# --- Domains (DNS A record → VPS IP) ---
POS_DOMAIN=steakandmarrow.inventivelab.bd
# S3 storage is the shared MinIO (s3.storage.inventivelab.bd) — see S3_ENDPOINT below.
ACME_EMAIL=admin@inventivelab.bd

# --- Postgres ---
POSTGRES_USER=krunch
POSTGRES_PASSWORD=change-me-strong-postgres-password
POSTGRES_DB=krunch

# --- API (also used by api container) ---
JWT_ACCESS_SECRET=change-me-generate-with-openssl-rand-hex-32
JWT_REFRESH_SECRET=change-me-generate-with-openssl-rand-hex-32
JWT_ACCESS_EXPIRY=900
JWT_REFRESH_EXPIRY=28800
CORS_ORIGIN=https://steakandmarrow.inventivelab.bd
GOOGLE_CLIENT_IDS=
OVERRIDE_TOKEN_EXPIRY=60

# --- Object storage (shared MinIO already running on the VPS) ---
# No local MinIO container; use a scoped MinIO user (e.g. krunch-app) limited to the krunch-pos bucket.
S3_ENDPOINT=https://s3.storage.inventivelab.bd
S3_PUBLIC_ENDPOINT=https://s3.storage.inventivelab.bd
S3_REGION=us-east-1
S3_BUCKET=krunch-pos
S3_ACCESS_KEY=krunch-app
S3_SECRET_KEY=change-me-krunch-app-minio-secret
S3_FORCE_PATH_STYLE=true
S3_PRESIGN_EXPIRY_SECONDS=900
# Stable public base for uploaded files (served via GET /api/v1/media/:id, proxied at /media/:id on S3 host).
MEDIA_PUBLIC_BASE_URL=https://s3.storage.inventivelab.bd/media

# --- POS build (terminal production bundle; override before deploy when DNS is not live) ---
# VITE_API_URL=http://YOUR_VPS_IP/api/v1
GENERIC_TIMEZONE=Asia/Dhaka

# --- VPS deploy (Mac → server; scripts/deploy-to-vps.sh reads these) ---
VPS_HOST=194.164.91.252
VPS_USER=root
VPS_PASSWORD=change-me-vps-root-password
REMOTE_DIR=/opt/krunch-pos

# --- POS build defaults (terminal production bundle) ---
VITE_DEFAULT_BRANCH_ID=a0000000-0000-4000-8000-000000000001
VITE_DEFAULT_TERMINAL_ID=terminal-prod-001
